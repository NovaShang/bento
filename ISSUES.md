# Issues

## 浮动导航按钮

- **问题**：跳转到底部的按钮不见了。
- **需求**：在右侧添加两个浮动导航按钮：
  1. **向上箭头**：跳转到上一条用户消息。
  2. **向下箭头**：跳转到下一条用户消息；如果没有下条用户消息，则跳转到底部。

## Parallel 模式窗口最大化按钮改为 Focus 按钮

- **问题**：当前右上角的最大化按钮需要替换。
- **需求**：将 parallel 模式窗口右上角的最大化按钮改为 focus 按钮。点击后切换到 focus 模式，并自动选中与 parallel 模型一致的当前 pane。删除以前关于最大化的逻辑。

## Session 更名为 Workspace

- **问题**：产品中继承自 tmux 的 session 概念需要更名。
- **需求**：将整个产品中的 "session" 概念统一更名为 "workspace"，包括代码、UI 文案、文档等所有出现的地方。

## History 按钮复用当前 Pane

- **问题**：Pane 右上角的 history 按钮点击后会分出新的 pane。
- **需求**：History 应该在当前 pane 中打开，而不是分出新的 pane。