// ACPHostKit consumers spoke the link vocabulary (AcpByteLink, the byte
// links, AcpSealedConfig) through this module before the link layer became
// its own target — keep that reading true.
@_exported import BentoLink
