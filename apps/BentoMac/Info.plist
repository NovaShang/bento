<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
  Merged into the Xcode-generated Info.plist (GENERATE_INFOPLIST_FILE stays on,
  so every INFOPLIST_KEY_* setting in project.yml still applies). This file
  exists only for keys that have no INFOPLIST_KEY_ form — today, the URL
  scheme.

  `bento-acp://` is the launch ABI a resident menu process uses to reach this
  app: it works whether or not the app is running, wherever it is installed,
  and fails harmlessly when it isn't installed at all.
  See docs/menubar-unification.md §4.1.
-->
<plist version="1.0">
<dict>
	<key>CFBundleURLTypes</key>
	<array>
		<dict>
			<key>CFBundleTypeRole</key>
			<string>Editor</string>
			<key>CFBundleURLName</key>
			<string>com.bento.menubar.acp</string>
			<key>CFBundleURLSchemes</key>
			<array>
				<string>bento-acp</string>
			</array>
		</dict>
	</array>
</dict>
</plist>
