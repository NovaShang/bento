{
  "name": "bento-relay",
  "private": true,
  "version": "0.0.1",
  "description": "Cloudflare Worker + Durable Object that bridges iOS ↔ bento-daemon over WSS. Stateless byte-pump; flows are end-to-end encrypted by SSH.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250121.0",
    "typescript": "^5.6.3",
    "wrangler": "^3.99.0"
  }
}
